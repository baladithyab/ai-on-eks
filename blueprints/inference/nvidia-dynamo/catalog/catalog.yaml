# NVIDIA Dynamo example catalog (single source of truth)
#
# This file is intentionally simple YAML so it can be parsed reliably from bash
# without external dependencies.
#
# Fields:
# - id: stable example ID used by ./deploy.sh ./test.sh ./cleanup.sh
# - path: YAML path relative to ai-on-eks/blueprints/inference/nvidia-dynamo/
# - backend: vllm | sglang | trtllm | demo | infra
# - tier: core | standard | advanced | experimental | model-showcase
# - prereqs: short prerequisites string
# - notes: short notes string
# - validation_status: validated | experimental | blocked | pending (v0.7.1 results)
# - hardware: recommended instance type
# - cost_per_hour: estimated AWS cost (USD/hour)
# - test_date: v0.7.1 validation date (YYYY-MM-DD)
#
# Physical Layout:
#   engines/       - Base serving engine examples (vLLM, SGLang, TRT-LLM)
#   features/      - Cross-cutting features (autoscaling, KVBM, DGDR, multimodal, etc.)
#   models/        - Model-specific showcases (DeepSeek, GPT-OSS, Llama)
#   observability/ - Metrics, tracing, audit logging examples
#   experimental/  - Bleeding-edge and unstable features
#   config/        - Shared configuration components

entries:
  # ---------------------------------------------------------------------------
  # CORE (golden path)
  # v0.7.1 Validation: 10/11 PASSED (91%) - hello-world removed (requires etcd)
  # ---------------------------------------------------------------------------

  - id: vllm-aggregated-default
    path: engines/vllm/vllm-aggregated-default.yaml
    backend: vllm
    tier: core
    prereqs: "GPU nodes; hf-token-secret + ngc-secret in dynamo"
    notes: "Baseline aggregated serving (vLLM) - TP=1 validated on g6e"
    validation_status: validated
    hardware: "g6e.2xlarge+ (single GPU)"
    cost_per_hour: "$1.12+"
    test_date: "2026-01-01"

  - id: sglang-aggregated-default
    path: engines/sglang/sglang-aggregated-default.yaml
    backend: sglang
    tier: core
    prereqs: "GPU nodes; hf-token-secret + ngc-secret in dynamo"
    notes: "Backend diversity: minimal SGLang 'first success' - Works on PCIe"
    validation_status: validated
    hardware: "g6e.2xlarge+ (single GPU)"
    cost_per_hour: "$1.12+"
    test_date: "2026-01-01"

  - id: trtllm-aggregated-default
    path: engines/trtllm/trtllm-aggregated-default.yaml
    backend: trtllm
    tier: core
    prereqs: "GPU nodes; hf-token-secret + ngc-secret in dynamo"
    notes: "Backend diversity: minimal TensorRT-LLM 'first success'"
    validation_status: validated
    hardware: "g6e.2xlarge+ (single GPU)"
    cost_per_hour: "$1.12+"
    test_date: "2026-01-01"

  - id: vllm-disaggregated-default
    path: engines/vllm/vllm-disaggregated-default.yaml
    backend: vllm
    tier: core
    prereqs: "GPU nodes; hf-token-secret + ngc-secret in dynamo"
    notes: "Baseline disaggregated prefill/decode - TP=1 validated"
    validation_status: validated
    hardware: "g6e.2xlarge+ (2+ GPUs total)"
    cost_per_hour: "$2.24+"
    test_date: "2026-01-01"

  - id: vllm-router
    path: engines/vllm/vllm-router.yaml
    backend: vllm
    tier: core
    prereqs: "GPU nodes; hf-token-secret + ngc-secret in dynamo"
    notes: "KV-aware routing (router-mode=kv)"
    validation_status: validated
    hardware: "g6e.2xlarge+ (2+ GPUs)"
    cost_per_hour: "$2.24+"
    test_date: "2026-01-01"

  - id: vllm-disaggregated-kvbm-disk
    path: features/kvbm/vllm-disaggregated-kvbm-disk.yaml
    backend: vllm
    tier: core
    prereqs: "GPU nodes; enough RAM + ephemeral disk; hf-token-secret + ngc-secret"
    notes: "KVBM multi-tier KV cache (GPU→CPU→Disk)"
    validation_status: validated
    hardware: "g6e.12xlarge (4x GPU, high RAM)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  - id: multi-replica-vllm
    path: features/multi-replica/multi-replica-vllm.yaml
    backend: vllm
    tier: core
    prereqs: "GPU nodes; hf-token-secret + ngc-secret in dynamo"
    notes: "Multi-replica HA pattern + KV routing"
    validation_status: validated
    hardware: "g6e.2xlarge+ (2+ GPUs)"
    cost_per_hour: "$2.24+"
    test_date: "2026-01-01"

  - id: vllm-multinode-lws
    path: features/multinode/vllm-multinode-lws.yaml
    backend: vllm
    tier: core
    prereqs: "2+ Nodes; LWS installed"
    notes: "Multinode inference using LeaderWorkerSet (LWS)"
    validation_status: validated
    hardware: "g6e.2xlarge+ (2+ Nodes)"
    cost_per_hour: "$2.24+"
    test_date: "2026-01-01"

  - id: vllm-full-observability
    path: observability/vllm-full-observability.yaml
    backend: vllm
    tier: core
    prereqs: "GPU nodes; Tempo/OTEL stack (observability namespace)"
    notes: "All-in-one: metrics + logs + tracing + audit"
    validation_status: validated
    hardware: "g6e.2xlarge+ (single GPU)"
    cost_per_hour: "$1.12+"
    test_date: "2026-01-01"

  - id: llava-1.5-7b
    path: features/multimodal/llava-1.5-7b.yaml
    backend: vllm
    tier: core
    prereqs: "3 GPUs total (Encode+VLM+Processor); hf-token-secret + ngc-secret"
    notes: "Multimodal image understanding (LLaVA 1.5)"
    validation_status: validated
    hardware: "g6e.12xlarge (3+ GPUs)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  - id: llava-next-video-7b
    path: features/multimodal/llava-next-video-7b.yaml
    backend: vllm
    tier: core
    prereqs: "3 GPUs total; large CPU+RAM; hf-token-secret + ngc-secret"
    notes: "Multimodal video pipeline + KVBM long-context caching"
    validation_status: validated
    hardware: "g6e.12xlarge (3+ GPUs, high RAM)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  # Infra (showcase, not deploy.sh-first by default)
  - id: base-model
    path: features/model-management/base-model.yaml
    backend: N/A  # Reference CRD only
    tier: documentation  # CHANGE FROM: core
    category: model-management
    prereqs: "Requires serving DGD with modelRef"
    notes: "Reference CRD - does not create deployment"
    validation_status: pending
    hardware: "N/A"
    cost_per_hour: "$0.00"

  - id: lora-adapter
    path: features/model-management/lora-adapter.yaml
    backend: N/A  # Reference CRD only
    tier: documentation  # CHANGE FROM: core
    category: model-management
    prereqs: "Requires serving DGD with modelRef + base model"
    notes: "Reference CRD - does not create deployment"
    validation_status: pending
    hardware: "N/A"
    cost_per_hour: "$0.00"

  # Backwards-compatible aliases (keep existing docs/workflows working)
  - id: vllm
    path: engines/vllm/vllm-aggregated-default.yaml
    backend: vllm
    tier: core
    prereqs: "Alias for vllm-aggregated-default"
    notes: "Compatibility ID (use vllm-aggregated-default in docs)"
    validation_status: validated
    hardware: "g6e.2xlarge+"
    cost_per_hour: "$1.12+"
    test_date: "2026-01-01"

  - id: sglang
    path: engines/sglang/sglang-aggregated-default.yaml
    backend: sglang
    tier: core
    prereqs: "Alias for sglang-aggregated-default"
    notes: "Compatibility ID (use sglang-aggregated-default in docs)"
    validation_status: validated
    hardware: "g6e.2xlarge+"
    cost_per_hour: "$1.12+"
    test_date: "2026-01-01"

  - id: trtllm
    path: engines/trtllm/trtllm-aggregated-default.yaml
    backend: trtllm
    tier: core
    prereqs: "Alias for trtllm-aggregated-default"
    notes: "Compatibility ID (use trtllm-aggregated-default in docs)"
    validation_status: validated
    hardware: "g6e.2xlarge+"
    cost_per_hour: "$1.12+"
    test_date: "2026-01-01"

  # ---------------------------------------------------------------------------
  # STANDARD (common variants)
  # v0.7.1 Validation: 11/11 PASSED (100%)
  # ---------------------------------------------------------------------------
  - id: sglang-disaggregated-default
    path: engines/sglang/sglang-disaggregated-default.yaml
    backend: sglang
    tier: standard
    prereqs: "GPU nodes; hf-token-secret + ngc-secret"
    notes: "SGLang disaggregated prefill/decode - Works on PCIe (TP>1)"
    validation_status: validated
    hardware: "g6e.12xlarge (4x GPU)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  - id: trtllm-disaggregated-default
    path: engines/trtllm/trtllm-disaggregated-default.yaml
    backend: trtllm
    tier: standard
    prereqs: "GPU nodes; hf-token-secret + ngc-secret"
    notes: "TensorRT-LLM disaggregated prefill/decode"
    validation_status: validated
    hardware: "g6e.12xlarge (4x GPU)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  - id: sglang-router
    path: engines/sglang/sglang-router.yaml
    backend: sglang
    tier: standard
    prereqs: "GPU nodes; hf-token-secret + ngc-secret"
    notes: "KV-aware routing (SGLang)"
    validation_status: validated
    hardware: "g6e.12xlarge (4x GPU)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  - id: trtllm-router
    path: engines/trtllm/trtllm-router.yaml
    backend: trtllm
    tier: standard
    prereqs: "GPU nodes; hf-token-secret + ngc-secret"
    notes: "KV-aware routing (TensorRT-LLM)"
    validation_status: validated
    hardware: "g6e.12xlarge (4x GPU)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  - id: vllm-aggregated-kvbm
    path: features/kvbm/vllm-aggregated-kvbm.yaml
    backend: vllm
    tier: standard
    prereqs: "GPU nodes; extra CPU RAM; hf-token-secret + ngc-secret"
    notes: "KVBM (aggregated)"
    validation_status: validated
    hardware: "g6e.12xlarge (high RAM)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  - id: vllm-aggregated-router
    path: engines/vllm/vllm-aggregated-router.yaml
    backend: vllm
    tier: standard
    prereqs: "GPU nodes; hf-token-secret + ngc-secret"
    notes: "vLLM aggregated + router variant"
    validation_status: validated
    hardware: "g6e.12xlarge (4x GPU)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  - id: vllm-disaggregated-router
    path: engines/vllm/vllm-disaggregated-router.yaml
    backend: vllm
    tier: standard
    prereqs: "GPU nodes; hf-token-secret + ngc-secret"
    notes: "vLLM disaggregated + router variant"
    validation_status: validated
    hardware: "g6e.12xlarge (4x GPU)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  - id: vllm-otel-tracing
    path: observability/vllm-otel-tracing.yaml
    backend: vllm
    tier: standard
    prereqs: "GPU nodes; Tempo/OTEL stack"
    notes: "Tracing-only variant"
    validation_status: validated
    hardware: "g6e.2xlarge+ (single GPU)"
    cost_per_hour: "$1.12+"
    test_date: "2026-01-01"

  - id: vllm-audit-logging
    path: observability/vllm-audit-logging.yaml
    backend: vllm
    tier: standard
    prereqs: "GPU nodes"
    notes: "Audit-logging-only variant"
    validation_status: validated
    hardware: "g6e.2xlarge+ (single GPU)"
    cost_per_hour: "$1.12+"
    test_date: "2026-01-01"

  - id: qwen2.5-vl-7b
    path: features/multimodal/qwen2.5-vl-7b.yaml
    backend: vllm
    tier: standard
    prereqs: "3 GPUs total; high memory; hf-token-secret + ngc-secret"
    notes: "Multimodal image (Qwen2.5-VL)"
    validation_status: validated
    hardware: "g6e.12xlarge (3+ GPUs, high RAM)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  - id: trtllm-aggregated-high-performance
    path: engines/trtllm/trtllm-aggregated-high-performance.yaml
    backend: trtllm
    tier: standard
    prereqs: "GPU nodes; hf-token-secret + ngc-secret"
    notes: "Aggressive TRT-LLM tuning"
    validation_status: validated
    hardware: "g6e.12xlarge (4x GPU)"
    cost_per_hour: "$3.91"
    test_date: "2026-01-01"

  # ---------------------------------------------------------------------------
  # ADVANCED (profiling/planner + large models)
  # v0.7.1 Validation: Not fully tested (requires NVLink/profiling infrastructure)
  # ---------------------------------------------------------------------------
  - id: vllm-disaggregated-planner
    path: features/dgdr-planner/vllm-disaggregated-planner.yaml
    backend: vllm
    tier: advanced
    prereqs: "Planner + profiling artifacts; GPU nodes"
    notes: "SLA planner (note: manifest metadata.name may differ)"
    validation_status: pending
    hardware: "g6e.12xlarge+ (requires profiling)"
    cost_per_hour: "$3.91+"

  - id: sglang-planner
    path: features/dgdr-planner/sglang-planner.yaml
    backend: sglang
    tier: advanced
    prereqs: "Planner + profiling artifacts; GPU nodes"
    notes: "SGLang SLA planner"
    validation_status: pending
    hardware: "g6e.12xlarge+ (requires profiling)"
    cost_per_hour: "$3.91+"

  - id: trtllm-planner
    path: features/dgdr-planner/trtllm-planner.yaml
    backend: trtllm
    tier: advanced
    prereqs: "Planner + profiling artifacts; GPU nodes"
    notes: "TensorRT-LLM SLA planner"
    validation_status: pending
    hardware: "g6e.12xlarge+ (requires profiling)"
    cost_per_hour: "$3.91+"

  - id: vllm-dgdr-online
    path: features/dgdr-planner/vllm-dgdr-online.yaml
    backend: vllm
    tier: advanced
    prereqs: "Prometheus stack; hours of profiling; GPU nodes"
    notes: "DGDR online profiling (AIPerf)"
    validation_status: pending
    hardware: "g6e.12xlarge+ (requires profiling)"
    cost_per_hour: "$3.91+"

  - id: sglang-dgdr-online
    path: features/dgdr-planner/sglang-dgdr-online.yaml
    backend: sglang
    tier: advanced
    prereqs: "Prometheus stack; hours of profiling; GPU nodes"
    notes: "DGDR online profiling (SGLang) - Set DYNAMO_TRACE_ENABLED=false"
    validation_status: experimental
    hardware: "g6e.12xlarge+ (requires profiling)"
    cost_per_hour: "$3.91+"

  - id: trtllm-dgdr-online
    path: features/dgdr-planner/trtllm-dgdr-online.yaml
    backend: trtllm
    tier: advanced
    prereqs: "Prometheus stack; hours of profiling; GPU nodes"
    notes: "DGDR online profiling (TensorRT-LLM)"
    validation_status: pending
    hardware: "g6e.12xlarge+ (requires profiling)"
    cost_per_hour: "$3.91+"

  - id: vllm-dgdr-deepseek-32b
    path: models/deepseek/vllm-dgdr-deepseek-32b.yaml
    backend: vllm
    tier: advanced
    prereqs: "Prometheus stack; 2-4h profiling; multi-GPU"
    notes: "DGDR (32B class)"
    validation_status: pending
    hardware: "g6e.12xlarge+ (2-4 GPUs)"
    cost_per_hour: "$3.91+"

  - id: vllm-dgdr-qwen-coder-32b
    path: features/dgdr-planner/vllm-dgdr-qwen-coder-32b.yaml
    backend: vllm
    tier: advanced
    prereqs: "Prometheus stack; 2-4h profiling; multi-GPU"
    notes: "DGDR (32B class)"
    validation_status: pending
    hardware: "g6e.12xlarge+ (2-4 GPUs)"
    cost_per_hour: "$3.91+"

  - id: trtllm-dgdr-aic
    path: features/dgdr-planner/trtllm-dgdr-aic.yaml
    backend: trtllm
    tier: advanced
    prereqs: "AIC-capable hardware (H100/H200); Prometheus"
    notes: "DGDR with AI Configurator (fast simulation)"
    validation_status: pending
    hardware: "p5.48xlarge (H100 required)"
    cost_per_hour: "$98.32"

  - id: sglang-disaggregated-2gpu
    path: features/multi-replica/sglang-disaggregated-2gpu.yaml
    backend: sglang
    tier: advanced
    prereqs: "4+ GPUs total; hf-token-secret + ngc-secret"
    notes: "TP tuning example"
    validation_status: experimental
    hardware: "g6e.12xlarge (4+ GPUs)"
    cost_per_hour: "$3.91"

  - id: vllm-aggregated-gptoss-20b
    path: models/gpt-oss/vllm-aggregated-gptoss-20b.yaml
    backend: v llm
    tier: advanced
    prereqs: "Multi-GPU; large model download"
    notes: "Aggregated large model - Requires NVLink for TP>1"
    validation_status: blocked
    hardware: "p5.48xlarge (NVLink required for vLLM TP>1)"
    cost_per_hour: "$98.32"

  - id: vllm-disaggregated-gptoss-20b
    path: models/gpt-oss/vllm-disaggregated-gptoss-20b.yaml
    backend: vllm
    tier: advanced
    prereqs: "Multi-GPU; large model download"
    notes: "Disaggregated large model - Requires NVLink for TP>1"
    validation_status: blocked
    hardware: "p5.48xlarge (NVLink required for vLLM TP>1)"
    cost_per_hour: "$98.32"

  - id: vllm-disaggregated-gptoss-120b
    path: models/gpt-oss/vllm-disaggregated-gptoss-120b.yaml
    backend: vllm
    tier: advanced
    prereqs: "Many GPUs; long warmup"
    notes: "Very large model - Requires NVLink for TP>1"
    validation_status: blocked
    hardware: "p5.48xlarge (8x H100 NVLink)"
    cost_per_hour: "$98.32"

  - id: vllm-disaggregated-70b
    path: models/llama-family/vllm-disaggregated-70b.yaml
    backend: vllm
    tier: advanced
    prereqs: "Many GPUs; large storage"
    notes: "70B disaggregated - Requires NVLink for TP>1"
    validation_status: blocked
    hardware: "p5.48xlarge (NVLink required)"
    cost_per_hour: "$98.32"

  - id: vllm-disaggregated-deepseek-70b
    path: models/deepseek/vllm-disaggregated-deepseek-70b.yaml
    backend: vllm
    tier: advanced
    prereqs: "Many GPUs; large storage"
    notes: "70B disaggregated (DeepSeek distilled) - Requires NVLink for TP>1"
    validation_status: blocked
    hardware: "p5.48xlarge (NVLink required)"
    cost_per_hour: "$98.32"

  # ---------------------------------------------------------------------------
  # EXPERIMENTAL (heavy DGDR)
  # v0.7.1 Validation: Not tested (requires specialized infrastructure)
  # NOTE: Multi-node examples removed until Grove/Kai is validated.
  # ---------------------------------------------------------------------------
  - id: vllm-dgdr-deepseek-70b
    path: experimental/vllm-dgdr-deepseek-70b.yaml
    backend: vllm
    tier: experimental
    prereqs: "Prometheus; very long profiling; many GPUs"
    notes: "DGDR 70B (experimental/unstable)"
    validation_status: experimental
    hardware: "p5.48xlarge (many GPUs)"
    cost_per_hour: "$98.32+"

  - id: vllm-dgdr-deepseek-70b-g6
    path: experimental/vllm-dgdr-deepseek-70b-g6.yaml
    backend: vllm
    tier: experimental
    prereqs: "Prometheus; very long profiling; many GPUs; GPU type-specific"
    notes: "DGDR 70B variant (g6 tuning)"
    validation_status: experimental
    hardware: "g6e.48xlarge (specific tuning)"
    cost_per_hour: "$15.64+"

  # NOTE: Multi-node examples (Grove/KAI, LWS) removed until Grove/Kai is validated.
  # See issue tracking at: https://github.com/awslabs/ai-on-eks/issues/XXX

  # ---------------------------------------------------------------------------
  # MODEL SHOWCASE
  # v0.7.1 Validation: 3 model families tested (6 blueprints)
  # This tier focuses on model families rather than Dynamo features
  # ---------------------------------------------------------------------------

  # GPT-OSS (NVIDIA) - Production-ready reasoning models
  - id: showcase-gptoss-20b-vllm
    path: models/gpt-oss/vllm-aggregated-gptoss-20b.yaml
    backend: vllm
    tier: model-showcase
    category: GPT-OSS
    prereqs: "4x GPUs (p5.48xlarge NVLink); EFS shared cache; hf-token-secret"
    notes: "NVIDIA GPT-OSS 20B - Blocked on PCIe (vLLM TP>1 limitation)"
    validation_status: blocked
    hardware: "p5.48xlarge (NVLink required for vLLM TP>1)"
    cost_per_hour: "$98.32"
    test_date: "2025-12-28"

  - id: showcase-gptoss-20b-sglang-agg
    path: models/gpt-oss/sglang-aggregated-gptoss-20b.yaml
    backend: sglang
    tier: model-showcase
    category: GPT-OSS
    prereqs: "4x GPUs (g6e.12xlarge); EFS shared cache; hf-token-secret"
    notes: "NVIDIA GPT-OSS 20B aggregated (SGLang) - Works on PCIe"
    validation_status: validated
    hardware: "g6e.12xlarge (4x L40S PCIe)"
    cost_per_hour: "$3.91"
    test_date: "2025-12-28"

  - id: showcase-gptoss-20b-sglang-dgd
    path: models/gpt-oss/sglang-disaggregated-gptoss-20b.yaml
    backend: sglang
    tier: model-showcase
    category: GPT-OSS
    prereqs: "4x GPUs (g6e.12xlarge); EFS shared cache; hf-token-secret"
    notes: "NVIDIA GPT-OSS 20B disaggregated (SGLang) - Works on PCIe"
    validation_status: validated
    hardware: "g6e.12xlarge (4x L40S PCIe)"
    cost_per_hour: "$3.91"
    test_date: "2025-12-28"

  - id: showcase-gptoss-20b-sglang-router
    path: models/gpt-oss/sglang-router-gptoss-20b.yaml
    backend: sglang
    tier: model-showcase
    category: GPT-OSS
    prereqs: "4x GPUs (g6e.12xlarge); EFS shared cache; hf-token-secret"
    notes: "NVIDIA GPT-OSS 20B with KV routing (SGLang) - Works on PCIe"
    validation_status: validated
    hardware: "g6e.12xlarge (4x L40S PCIe)"
    cost_per_hour: "$3.91"
    test_date: "2025-12-28"

  - id: showcase-gptoss-120b-vllm
    path: models/gpt-oss/vllm-disaggregated-gptoss-120b.yaml
    backend: vllm
    tier: model-showcase
    category: GPT-OSS
    prereqs: "8x GPUs (p5.48xlarge); EFS shared cache; hf-token-secret"
    notes: "NVIDIA GPT-OSS 120B large-scale production model - Requires NVLink"
    validation_status: blocked
    hardware: "p5.48xlarge (8x H100 NVLink)"
    cost_per_hour: "$98.32"

  - id: showcase-gptoss-120b-sglang
    path: models/gpt-oss/sglang-aggregated-gptoss-120b.yaml
    backend: sglang
    tier: model-showcase
    category: GPT-OSS
    prereqs: "8x GPUs (p5.48xlarge); EFS shared cache; hf-token-secret"
    notes: "NVIDIA GPT-OSS 120B (SGLang) - Not tested (hardware unavailable)"
    validation_status: pending
    hardware: "p5.48xlarge (8x H100)"
    cost_per_hour: "$98.32"

  # DeepSeek (Community) - Advanced reasoning capabilities
  - id: showcase-deepseek-r1-8b
    path: models/deepseek/sglang-deepseek-r1-distill-8b.yaml
    backend: sglang
    tier: model-showcase
    category: DeepSeek
    prereqs: "1x GPU (g5.2xlarge); EFS shared cache; hf-token-secret"
    notes: "DeepSeek R1-Distill 8B reasoning specialist (single GPU)"
    validation_status: validated
    hardware: "g5.2xlarge (1x A10G)"
    cost_per_hour: "$1.21"
    test_date: "2025-12-28"

  - id: showcase-deepseek-32b
    path: models/deepseek/vllm-dgdr-deepseek-32b.yaml
    backend: vllm
    tier: model-showcase
    category: DeepSeek
    prereqs: "2x GPUs; Prometheus for DGDR; EFS shared cache"
    notes: "DeepSeek R1-Distill 32B with DGDR profiling"
    validation_status: pending
    hardware: "g6e.12xlarge (2+ GPUs)"
    cost_per_hour: "$3.91"

  - id: showcase-deepseek-70b-vllm
    path: models/deepseek/vllm-disaggregated-deepseek-70b.yaml
    backend: vllm
    tier: model-showcase
    category: DeepSeek
    prereqs: "8x GPUs (p5.48xlarge); EFS shared cache; hf-token-secret"
    notes: "DeepSeek R1-Distill 70B full reasoning - Requires NVLink for vLLM TP>1"
    validation_status: blocked
    hardware: "p5.48xlarge (8x H100 NVLink)"
    cost_per_hour: "$98.32"

  - id: showcase-deepseek-70b-sglang-agg
    path: models/deepseek/sglang-aggregated-deepseek-70b.yaml
    backend: sglang
    tier: model-showcase
    category: DeepSeek
    prereqs: "8x GPUs (p5.48xlarge); EFS shared cache; hf-token-secret"
    notes: "DeepSeek R1-Distill 70B aggregated (SGLang) - Not tested"
    validation_status: pending
    hardware: "p5.48xlarge (8x H100)"
    cost_per_hour: "$98.32"

  - id: showcase-deepseek-70b-sglang-dgd
    path: models/deepseek/sglang-disaggregated-deepseek-70b.yaml
    backend: sglang
    tier: model-showcase
    category: DeepSeek
    prereqs: "8x GPUs (p5.48xlarge); EFS shared cache; hf-token-secret"
    notes: "DeepSeek R1-Distill 70B disaggregated (SGLang) - Not tested"
    validation_status: pending
    hardware: "p5.48xlarge (8x H100)"
    cost_per_hour: "$98.32"

  - id: showcase-deepseek-70b-sglang-router
    path: models/deepseek/sglang-router-deepseek-70b.yaml
    backend: sglang
    tier: model-showcase
    category: DeepSeek
    prereqs: "8x GPUs (p5.48xlarge); EFS shared cache; hf-token-secret"
    notes: "DeepSeek R1-Distill 70B with KV routing (SGLang) - Not tested"
    validation_status: pending
    hardware: "p5.48xlarge (8x H100)"
    cost_per_hour: "$98.32"

  # Llama Family (Meta) - Foundation models
  - id: showcase-llama-3.3-70b-vllm
    path: models/llama-family/vllm-llama-3.3-70b.yaml
    backend: vllm
    tier: model-showcase
    category: Llama
    prereqs: "8x GPUs (p5.48xlarge); Meta license accepted; EFS shared cache"
    notes: "Meta Llama 3.3-70B flagship - Requires NVLink for vLLM TP>1"
    validation_status: blocked
    hardware: "p5.48xlarge (8x H100 NVLink)"
    cost_per_hour: "$98.32"

  - id: showcase-llama-3.3-70b-sglang-agg
    path: models/llama-family/sglang-aggregated-llama-3.3-70b.yaml
    backend: sglang
    tier: model-showcase
    category: Llama
    prereqs: "8x GPUs (p5.48xlarge); Meta license accepted; EFS shared cache"
    notes: "Meta Llama 3.3-70B aggregated (SGLang) - Not tested"
    validation_status: pending
    hardware: "p5.48xlarge (8x H100)"
    cost_per_hour: "$98.32"

  - id: showcase-llama-3.3-70b-sglang-dgd
    path: models/llama-family/sglang-disaggregated-llama-3.3-70b.yaml
    backend: sglang
    tier: model-showcase
    category: Llama
    prereqs: "8x GPUs (p5.48xlarge); Meta license accepted; EFS shared cache"
    notes: "Meta Llama 3.3-70B disaggregated (SGLang) - Not tested"
    validation_status: pending
    hardware: "p5.48xlarge (8x H100)"
    cost_per_hour: "$98.32"

  - id: showcase-llama-3.3-70b-sglang-router
    path: models/llama-family/sglang-router-llama-3.3-70b.yaml
    backend: sglang
    tier: model-showcase
    category: Llama
    prereqs: "8x GPUs (p5.48xlarge); Meta license accepted; EFS shared cache"
    notes: "Meta Llama 3.3-70B with KV routing (SGLang) - Not tested"
    validation_status: pending
    hardware: "p5.48xlarge (8x H100)"
    cost_per_hour: "$98.32"
