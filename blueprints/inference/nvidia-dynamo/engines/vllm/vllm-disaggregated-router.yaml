# SPDX-FileCopyrightText: Copyright (c) 2025 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
#
# Example: vLLM Disaggregated Deployment with KV Router Enabled
#
# This example demonstrates how to enable the KV Router feature (v0.5.0+) in a
# disaggregated deployment with separate prefill and decode workers.
#
# KV Router Benefits:
# - Improved cache hit rates for workloads with shared prompt prefixes
# - Reduced latency by routing requests to workers with relevant KV cache
# - Better GPU utilization through intelligent request routing
#
# For more information, see:
# - Dynamo docs: docs/benchmarks/kv-router-ab-testing.md
# - Upgrade guide: infra/nvidia-dynamo/UPGRADE_TO_V0.6.0.md

apiVersion: nvidia.com/v1alpha1
kind: DynamoGraphDeployment
metadata:
  name: vllm-disaggregated-router
  namespace: dynamo-system
spec:
  backendFramework: vllm
  pvcs:
  - name: dynamo-model-cache
    create: false
  services:
    Frontend:
      envFromSecret: hf-token-secret
      volumeMounts:
      - name: dynamo-model-cache
        mountPoint: /models
      componentType: frontend
      replicas: 1
      resources:
        requests:
          cpu: "2"
          memory: 4Gi
        limits:
          cpu: "2"
          memory: 4Gi
      extraPodSpec:
        mainContainer:
          image: nvcr.io/nvidia/ai-dynamo/dynamo-frontend:1.0.1
          workingDir: /workspace/components/backends/vllm
          command:
          - /bin/sh
          - -c
          args:
            # Enable KV Router by setting --router-mode kv
          - python3 -m dynamo.frontend --http-port 8000 --router-mode kv
      envs:
        # Alternative: Set via environment variable
      - name: DYN_ROUTER_MODE
        value: kv
      - name: HF_HOME
        value: /models
      - name: HF_HUB_CACHE
        value: /models
    VllmDecodeWorker:
      envFromSecret: hf-token-secret
      volumeMounts:
      - name: dynamo-model-cache
        mountPoint: /models
      envs:
      - name: HF_HOME
        value: /models
      - name: HF_HUB_CACHE
        value: /models
      componentType: worker
      subComponentType: decode
      replicas: 2  # Multiple replicas to demonstrate KV Router benefits
      resources:
        requests:
          cpu: "10"
          memory: 20Gi
          gpu: "1"
        limits:
          cpu: "10"
          memory: 20Gi
          gpu: "1"
      extraPodSpec:
        nodeSelector:
          karpenter.sh/nodepool: g5-nvidia
        mainContainer:
          image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.0.1
          workingDir: /workspace/components/backends/vllm
          command:
          - /bin/sh
          - -c
          args:
            # Enable prefix caching for better KV Router performance
          - python3 -m dynamo.vllm --model Qwen/Qwen3-0.6B --enable-prefix-caching
    VllmPrefillWorker:
      envFromSecret: hf-token-secret
      volumeMounts:
      - name: dynamo-model-cache
        mountPoint: /models
      envs:
      - name: HF_HOME
        value: /models
      - name: HF_HUB_CACHE
        value: /models
      componentType: worker
      subComponentType: prefill
      replicas: 1
      resources:
        requests:
          cpu: "10"
          memory: 20Gi
          gpu: "1"
        limits:
          cpu: "10"
          memory: 20Gi
          gpu: "1"
      extraPodSpec:
        nodeSelector:
          karpenter.sh/nodepool: g5-nvidia
        mainContainer:
          image: nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.0.1
          workingDir: /workspace/components/backends/vllm
          command:
          - /bin/sh
          - -c
          args:
            # Enable prefix caching and mark as prefill worker
          - |
            python3 -m dynamo.vllm \
              --model Qwen/Qwen3-0.6B \
              --disaggregation-mode prefill \
              --kv-transfer-config '{"kv_connector":"NixlConnector","kv_role":"kv_both"}' \
              --enable-prefix-caching
